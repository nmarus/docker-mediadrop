### docker-mediadrop
#####Mediadrop on Docker with Nginx, uWSGI, and MariaDB

######Install/Run:

1. Edit docker-compose.yml
2. Build 

        $ ./build.sh /all

3. Run

        $ docker-compose up -d

